<%- include('../partes/header') %>

<h1>Editar Gift Card</h1>



<div class="container mt-4">
  <h2 class="mb-4">Editar GiftCard</h2>

  <form id="formEditarGiftCard" enctype="multipart/form-data" class="row g-3">
    <input type="hidden" name="giftcardId" value="<%= giftcard.id %>">

    <div class="col-md-6">
      <label for="nombre" class="form-label">Nombre</label>
      <input type="text" id="nombre" name="nombre" class="form-control"
             value="<%= giftcard.nombre %>" required>
    </div>

    <div class="col-md-6">
      <label for="precio" class="form-label">Precio</label>
      <input type="number" id="precio" name="precio" step="0.01" class="form-control"
             value="<%= giftcard.precio %>" required>
    </div>

    <div class="col-md-6">
      <label for="empresa" class="form-label">Empresa</label>
      <input type="text" id="empresa" name="empresa" class="form-control"
             value="<%= giftcard.empresa %>" required>
    </div>

    <div class="col-md-6">
      <label for="consola" class="form-label">Consola</label>
      <input type="text" id="consola" name="consola" class="form-control"
             value="<%= giftcard.consola %>" required>
    </div>

    <div class="col-md-6">
      <label for="requerimientos_minimos" class="form-label">Requisitos Mínimos</label>
      <input type="text" id="requerimientos_minimos" name="requerimientos_minimos"
             class="form-control" value="<%= giftcard.requerimientos_minimos %>" required>
    </div>

    <div class="col-md-6">
      <label for="stock" class="form-label">Stock</label>
      <input type="number" id="stock" name="stock" class="form-control"
             value="<%= giftcard.stock %>" required>
    </div>

    <div class="col-md-6">
      <label for="fecha_caducidad" class="form-label">Fecha de Caducidad</label>
      <input type="date" id="fecha_caducidad" name="fecha_caducidad" class="form-control"
             value="<%= giftcard.fecha_caducidad.substring(0, 10) %>" required>
    </div>

    <div class="col-md-6">
      <label for="plataformas_disponibles" class="form-label">Plataformas Disponibles</label>
      <input type="text" id="plataformas_disponibles" name="plataformas_disponibles"
             class="form-control" value="<%= giftcard.plataformas_disponibles %>" required>
    </div>

    <div class="col-md-12">
      <label for="imagen" class="form-label">Imagen</label>
      <input type="file" id="imagen" name="imagen" class="form-control">
      <small class="form-text text-muted">
        Imagen actual: <%= giftcard.imagen ? giftcard.imagen : 'No disponible' %>
      </small>
    </div>

    <div class="col-12 d-flex justify-content-between mt-3">
      <a href="/admin/panelProductos" class="btn btn-secondary">Volver al listado</a>
      <button type="submit" class="btn btn-primary">Guardar Cambios</button>
    </div>
  </form>
</div>


<script>
    const token = '<%= token %>'; // Asegurate de pasar el token desde el router

    document.getElementById('formEditarGiftCard').addEventListener('submit', async function (e) {
        e.preventDefault();

        const form = e.target;
        const formData = new FormData(form);
        const giftcardId = formData.get('giftcardId');

        try {
            const response = await fetch(`/api/giftcards/${giftcardId}/modificar`, {
                method: 'PUT',
                headers: {
                    'Authorization': 'Bearer ' + token
                },
                body: formData
            });

            if (!response.ok) {
                let errorData;
                try {
                    errorData = await response.json();
                } catch {
                    errorData = { message: 'No se pudo interpretar el error del servidor' };
                }
                alert('Error: ' + (errorData.message || JSON.stringify(errorData)));
                return;
            }

            alert('GiftCard actualizada correctamente');
            window.location.href = '/admin/panelProductos'; // Redirige al listado

        } catch (err) {
            console.error(err);
            alert('Error de red o servidor: ' + err.message);
        }
    });
</script>

<%- include('../partes/footer') %>
